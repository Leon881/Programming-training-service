import React from 'react';
import { connect } from 'react-redux';
import LearningSQL from '../components/LearningSQL/LearningSQL';
import { navigateToPage } from '../actionCreators';

export default connect(
    (state,props) => ({

    }),
    (dispatch,props) =>({
    })
)(LearningSQL)